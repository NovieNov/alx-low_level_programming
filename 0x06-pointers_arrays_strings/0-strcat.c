#include "main.h"

/**
 * _strcat - Checks the code
 * @dest: The pointer
 * @src: The pointer
 *
 * return: returns dest
 */
char *_strcat(char *dest, char *src)
{
	int i, n;

	for (i = 0; dest[i] != '\0'; i++)
	{
	}

	for (n = 0; (dest[i + n] = *src++); n++)
	{
	}

	return dest;
}
