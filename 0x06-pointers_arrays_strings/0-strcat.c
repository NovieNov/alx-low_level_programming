#include "main.h"

/**
 * strcat - Checks the code
 * @dest: The pointer
 * @src: The pointer
 *
 * return: returns 0
 */
char *_strcat(char *dest, char *src)
{
	int i, n;

	for (i = 0; dest[i] != '\0'; i++)
	{
	}
	
	for (n = 0; (dest[i + n] = *src++) != '\0'; n++)
	{
	}

	return (dest);
}
