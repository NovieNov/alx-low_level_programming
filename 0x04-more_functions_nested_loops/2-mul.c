#include "main.h"

/**
 * mul - check for code
 * @a: a variable with type int
 * @b: a variable with type int
 * Return: multiplication of variables
 */
int mul(int a, int b)
{
	int c;

	c = (a * b);
	
	return (c);
}
