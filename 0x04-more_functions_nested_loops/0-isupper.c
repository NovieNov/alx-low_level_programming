#include "main.h"

/**
 * _putchar - Checks the code
 * @c: single special character
 *
 * Return: Always 0 (SUCCESS)
 */
int _putchar(char c)
{
	if (c >= 'C' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
